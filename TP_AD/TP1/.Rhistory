getwd()
mode(x)
x=5
mode(x)
x<-6
x
x->6
x->b
b
b<-4
b
clear()
cls
 x<- c(4,-2.3,9.5)
x
y<-c(x,3,c(12,8))
y
mode(y)
x[c(2,3)]
x[0]
x[1]
x[-c(1,4)]
x[7]
y[is.na(y)]<-0
y
y[3]<-x[7]
y
y[is.na(y)]<-0
y
x
2=x
2*x
x>3
which.min(x)
min(x)
y
which.min(y)
x+2:4
x
x+2:4
m<-matrix(c(1,18,12,3,4,5,3,9,7),byrow=TRUE,3,3)
m
 m[1:2,-1]
m[1,-c(1,3)]
m[2:3,-1]
which(m[,1] ==3)
m
which(m[,1] ==3)
m[which(m[,1] ==3),]
which(m ==3)
which(m ==4)
m
solve(m)
which(m ==2)
m*m
m%^%m
%m^%m
ls
ls()
b
m
x
y
mode(m)
getwd()
tabac
tabac = read.table("tabac.txt", header = T, row.names = 1)
tabac
tabac$Vente # permet de r´ecup´erer un vecteur avec toutes les valeurs
mean(tabac$Vente)
mean(tabac$Vente)
tabac = read.table("tabac.txt", header = T, row.names = 1)
#header: une valeur logique (FALSE ou TRUE) indicant si le fichier contient les noms des variables sur la 1ère ligne
tabac
tabac$Vente # permet de réecupérer un vecteur avec toutes les valeurs
mean(tabac$Vente)
tabac = read.table("tabac.txt", header = T, row.names = 1)
#header: une valeur logique (FALSE ou TRUE) indicant si le fichier contient les noms des variables sur la 1ère ligne
tabac
tabac$Vente # permet de réecupérer un vecteur avec toutes les valeurs
mean(tabac$Vente)
tabac$Prix
cor(tabac$Prix, tabac$Vente)
plot(tabac$Prix,tabac$Vente) # tracé simple
plot(tabac$Prix,tabac$Vente, main="Consommation de tabac en France",col="blue",lwd=2,xlab="Prix relatif de vente (en euros)",ylab="Nombre de cigarettes vendues(en milliards)") # tracé plus sophistiqué
reg = lm(Vente~Prix, data = tabac)
help(lm)
reg
reg$coefficients
abline(reg, col = "red")
plot(tabac$Prix,tabac$Vente, main="Consommation de tabac en France",col="blue",lwd=2,xlab="Prix relatif de vente (en euros)",ylab="Nombre de cigarettes vendues(en milliards)") # tracé plus sophistiqué
abline(reg, col = "red")
summary(reg)
reg$residuals
round(reg$residuals,3)
reg$fitted.values # les valeurs prédites par le modèle ^yi
tabac
tabac
reg$fitted.values # les valeurs prédites par le modèle ^yi
SCEr = sum(reg$residuals^2)
SCEr
tabac$Vente
ybar = mean(tabac$Vente)
ybar
SCEt = sum((tabac$Vente - ybar)^2)
SCEt
tabac$Vente - ybar
tabac$Vente
ybar
SCEm = sum((reg$fitted - ybar)^2) 
SCEm + SCEr === SCEt
SCEm + SCEr ===SCEt
SCEm + SCEr 
SCEt
R² = SCEm / SCEt
Rcarre = SCEm / SCEt
Rcarre
summary(reg)$r.squared
summary(reg)$sigma^2
n = length(tabac) 
n
n = length(tabac$Prix) 
n
sigmaChapeauCarre = (SCEr / (n-2) )
sigmaChapeauCarre
summary(reg)$coefficients
summary(reg)$coefficients[2,3]
help(predict)
q()
